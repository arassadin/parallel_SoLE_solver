#include"matrix_transponator.h"

double** transponate(double** m, m_size s)
{
	double** result=new double*[s.hor];
	for(int i=0; i<s.hor; i++)
		matrix[i]=new double[s.vert];
	for(int i=0; i<s.hor)
		for(int j=0; j<s.vert; j++)
			result[i][j]=m[j][i];
}
